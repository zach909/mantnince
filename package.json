{
  "name": "universal-backup-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-icons.js",
    "build": "npm run prebuild && vite build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "8.0.14"
  }
}
